# Feature Security Checklist

This checklist is the current repository-level index for feature security
status. It is not a substitute for the focused architecture, governance,
deployment, and runbook documents linked from `docs/security/README.md`.

Category: [Security assurance](../README.md#assurance).

## Current Feature Status

| Feature or boundary | Current security status | Evidence |
| --- | --- | --- |
| Customer registration | Email OTP is required before account creation; client-supplied account numbers and admin-domain customer emails are rejected; account numbers contain exactly 12 independently random decimal digits; one server-side SGD 100.00 welcome credit is committed with the customer row and recorded in `registration_credits` with HMAC-SHA256 integrity metadata | `app/auth/registration_otp.py`, `app/auth/services.py`, `tests/test_auth_registration_login.py` |
| Customer login and MFA | Password login uses generic errors, PBKDF2 password hashing, failed-attempt controls, encrypted TOTP seeds, recovery-code fallback, and TOTP replay prevention; new-device login notification is mandatory for newly authenticated customer browsers and failed notification delivery removes the known-device row so the next login retries | `app/auth/services.py`, `app/security/passwords.py`, `app/security/device_recognition.py`, `tests/test_auth_registration_login.py`, `tests/test_mfa_lifecycle.py`, `tests/test_new_device_login.py` |
| Customer sessions | Server-side sessions use opaque cookie identifiers, HMAC-signed payloads, absolute lifetime enforcement, risk drift handling, and scoped session termination | `app/security/sessions.py`, `tests/test_session_management.py`, `tests/test_session_absolute_lifetime.py`, `tests/test_session_risk_binding.py` |
| CSRF | Global Flask-WTF CSRF is expected on unsafe browser routes and is tracked by route inventories plus targeted runtime tests | `app/extensions.py`, `tests/test_route_inventory_security.py`, `tests/test_payup.py` |
| Password reset and manual recovery | Token and current-version recovery-code verifiers are HMAC-backed; reset token exchange is CSRF-protected and one-shot; legacy recovery-code HMAC rows are not accepted; manual recovery is request-only until root-admin review and maker-checker approval; request-list summaries omit the optional requester reason while the selected detail view exposes it for reviewer context | `app/auth/password_reset.py`, `tests/test_password_reset.py`, `tests/test_admin_manual_recovery.py`, `tests/test_admin_maker_checker.py` |
| Account freeze | Customer-initiated freeze requires TOTP, revokes other sessions, blocks sensitive actions, sends a security email, and produces an immediate alert | `app/auth/services.py`, `app/security/alerts.py`, `tests/test_account_security_actions.py`, `tests/test_audit_alerting.py` |
| Payee management | Payee lookup requires TOTP before recipient identity disclosure; self, duplicate, missing, expired, and cross-user paths fail closed | `app/banking/routes.py`, `tests/test_payee_management_security.py`, `tests/test_payee_idor.py` |
| Local Transfer | Final execution reloads the payee and pending record, validates owner/account/amount state, consumes a keyed pending-token verifier, locks rows in deterministic order, commits ledger movement with required audit, and enforces a per-user daily limit (recomputed under the same sender row lock as the balance check, so concurrent transfers cannot bypass it); the ledger commit (balance, transaction row, pending-transfer consumption, required success audit) is an explicit, self-contained commit boundary that completes before the best-effort customer notification step, so a notification failure can never roll back, mask, or otherwise affect an already-completed transfer | `app/banking/routes.py`, `app/banking/services.py`, `tests/test_local_transfer.py`, `tests/test_local_transfer_security.py` |
| PayUp | Senders must set a 2-128 character account-owned PayUp display nickname before lookup/amount/confirmation; lookup uses independent durable account/session/source/recipient limits; confirmation displays source account ending, sender nickname, recipient phone, and recipient PayUp nickname when set; raw phones and nicknames are not stored in audit metadata or pending session state; centralized fail-closed risk policy blocks unavailable, stale, and recent-sensitive-event state, avoids routine TOTP for in-cap quick payments, and conditionally requires TOTP for amounts outside quick-transfer caps; execution recomputes policy under lock; pending tokens are keyed verifiers; the ledger commit is an explicit boundary independent of the best-effort customer notification step, same as Local Transfer | `app/banking/routes.py`, `app/banking/services.py`, `tests/test_payup.py` |
| Top-up approval | Top-up approval links use selector/verifier tokens and no-store/origin-referrer headers; completion rechecks owner eligibility before TOTP, records required approval and credit audits, credits balance/ledger/request state in one transaction, and sends the routine deposit email only after commit according to the customer transfer-activity preference | `app/banking/routes.py`, `app/banking/services.py`, `app/banking/topup_tokens.py`, `tests/test_banking_topup.py` |
| Transfer limits | PayUp and Local Transfer each have an independent daily limit; limit changes require TOTP and CSRF; custom limits are server-side and database validated between SGD 100.00 and SGD 10000.00 with cents precision | `app/banking/routes.py`, `app/banking/services.py`, `tests/test_transfer_limits.py` |
| Transaction history and dispute reporting | Transaction list/detail and dispute creation re-check party ownership server-side on every request (not just the initial link); only one open/under-review dispute per transaction is allowed, enforced by an app-level check and a DB-level partial unique index; a plain-staff-only guard (`require_plain_staff_session`) reviews disputes in the admin app, excluding `admin`/`root_admin`, whose oversight is limited to the audit-log viewer; viewing the dispute queue/detail uses the same required (fail-closed) audit contract as status changes, so a read does not proceed if the audit row cannot be recorded; status transitions are atomic (a conditional update keyed on the current status) so a stale or concurrent transition cannot overwrite a newer decision, and `resolved`/`rejected` disputes are immutable | `app/web/routes.py`, `app/admin/routes.py`, `app/admin/services.py`, `app/models.py`, `tests/test_transaction_history_idor.py`, `tests/test_transaction_dispute_flow.py`, `tests/test_admin_dispute_queue_role_separation.py` |
| Monthly statement export | Opening/closing balance is derived from the live user balance and completed-transaction history rather than trusting a cached view; CSV export neutralizes formula-trigger characters even when hidden behind leading whitespace or control characters, and is rate-limited and audited per download | `app/banking/services.py`, `app/banking/routes.py`, `tests/test_monthly_statement.py`, `tests/test_statement_download_security.py` |
| Staff/admin login | Admin runtime is isolated from the customer app and accepts only active staff roles with approved and verified workplace email, password, and TOTP; only invalid TOTP submissions consume the durable MFA failure budget | `app/__init__.py`, `app/admin/services.py`, `tests/test_admin_isolation.py`, `tests/test_admin_dashboard_operations.py` |
| Root-admin bootstrap and allowlist | Production root-admin allowlists require exact environment-specific counts, approved domains, no duplicates, no defaults, and no placeholder or numeric placeholder identities | `config.py`, `app/admin/bootstrap.py`, `tests/test_config.py`, `tests/test_production_guard.py` |
| Staff/admin maker-checker | Highest-risk staff lifecycle and manual recovery changes require a requester and different active root-admin approver; target state, request state, and required audit rows are atomic for execution and terminal decisions such as rejection/cancellation | `app/admin/services.py`, `tests/test_admin_maker_checker.py` |
| Staff/admin self-service password and MFA change | Own-account password change requires current password, configured password length/policy checks, password-history checks, and TOTP step-up, then revokes every session for that account; own-account MFA reset stages a new secret in the signed session only (active secret untouched until a code from the new authenticator confirms it), so an abandoned reset never weakens login MFA | `app/admin/services.py`, `app/admin/routes.py`, `tests/test_admin_self_service_account.py` |
| Staff customer-support self-freeze unfreeze | Bank staff (not admin/root_admin) can unfreeze a customer's own voluntary self-freeze directly with a required, retained reason and TOTP step-up; scoped by the positive invariant that the target is a customer, frozen, and has no security lock reason, so automatic and manual/operational locks stay root-admin/maker-checker-only; blocks staff acting on their own linked customer identity | `app/admin/services.py`, `app/admin/routes.py`, `tests/test_admin_self_service_account.py` |
| Customer support ticket contact | Logged-in customers (including frozen ones) can submit a categorized support ticket; creation is durably rate-limited per user, and a plain-staff-only guard (`require_plain_staff_session`) reviews and triages tickets, excluding `admin`/`root_admin`; queue responses omit free-text description/resolution detail, ticket detail access uses required (fail-closed) audit, status transitions are atomic (conditional update keyed on current status), and resolution notes are never written into audit metadata | `app/web/routes.py`, `app/admin/services.py`, `app/admin/routes.py`, `app/models.py`, `tests/test_support_contact_flow.py`, `tests/test_admin_support_ticket_queue.py` |
| Account recovery request context | The anonymous, pre-login manual recovery request form accepts an optional free-text reason field for reviewer context; the reason is stored on the request row for root-admin review but is never written into audit metadata (only presence/length), and the generic anti-enumeration response is unaffected by whether a reason was supplied | `app/auth/password_reset.py`, `app/auth/forms.py`, `app/auth/schemas.py`, `tests/test_password_reset.py` |
| Audit and alerts | Audit metadata redacts sensitive values, audit chains are HMAC-linked, selected critical events are immediate alerts, and webhook delivery sanitizes payloads | `app/security/audit.py`, `app/security/alerts.py`, `tests/test_audit_alerting.py`, `tests/test_audit_metadata_sanitization.py` |
| Deployment and runtime boundaries | Customer/admin runtimes, database roles, Docker secrets, EC2-managed transaction-ledger HMAC keyrings, Tailscale private admin access, Cloudflare staging controls, Nginx TLS, and readiness gates are documented and tested from repo-controlled artifacts | `docs/DEPLOYMENT.md`, `docs/OPERATIONS.md`, `tests/test_deployment.py`, `tests/test_zero_trust_access_boundary.py`, `tests/test_tailscale_admin_access.py` |
| Browser E2E | Local-only Playwright tests exercise browser-rendered authentication, MFA, session, banking, and boundary regressions against a loopback Flask server; they do not prove live staging or production provider state | `tests/e2e/`, `tests/test_playwright_e2e_config.py` |

## Maintenance Rules

- When a feature changes authentication, authorization, CSRF, rate limiting,
  session state, audit metadata, alerting, banking ledger behavior, migration
  shape, deployment gates, or operator action, update this checklist or a more
  specific linked security document in the same change.
- Add or update a stale-documentation test when the documented security status
  could otherwise drift silently.
- Do not describe live provider state, branch protection, SonarQube results, or
  production deployment outcomes as current unless the change also includes the
  verification artifact or points to the external evidence owner.
- Use fake values in examples and tests. Never add production secrets, real
  customer data, raw session ids, CSRF tokens, TOTP seeds, recovery codes, JWTs,
  private keys, provider exports, or real infrastructure credentials.
