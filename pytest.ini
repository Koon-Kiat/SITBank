[pytest]
addopts = --strict-markers
markers =
    security: security regression tests
    deployment: deployment/configuration/security deployment checks
    slow: slower tests that are still required in full CI
    serial: tests that must not run concurrently
